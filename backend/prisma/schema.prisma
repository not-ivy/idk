generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model mooddata {
  id    Int @id @default(autoincrement())
  date  Int
  score Int
}
